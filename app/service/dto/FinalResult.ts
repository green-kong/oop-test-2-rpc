export class FinalResult {
    playerWinningCount: number;
    budget: number;

    constructor(playerWinningCount: number, budget: number) {
        this.playerWinningCount = playerWinningCount;
        this.budget = budget;
    }
}
